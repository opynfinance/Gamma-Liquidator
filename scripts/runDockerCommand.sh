#!/bin/bash

# Runs a command input as an environment variable.
# Allows docker image to run arbitrary commands by specifying them in the
# environment rather than the `docker run` command.
$RUN_COMMAND
